
package com.yad.musicstudio

/**
 * Extension methods untuk AudioEngine.
 * Gabung dengan AudioEngine via extension.
 */

fun AudioEngine.getCurrentStepPublic(): Int {
    // Akses field privat via reflection (atau ubah jadi public)
    // Untuk sekarang return -1 sampai kita expose field
    return -1
}
