<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme utama — extend dari Theme.AppCompat -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">#0F172A</item>
        <item name="android:statusBarColor">#1E293B</item>
        <item name="android:navigationBarColor">#1E293B</item>
        <item name="colorPrimary">#3B82F6</item>
        <item name="colorPrimaryDark">#1E40AF</item>
        <item name="colorAccent">#60A5FA</item>
    </style>

    <!-- Theme splash (opsional) -->
    <style name="AppTheme.Splash" parent="AppTheme">
        <item name="android:windowBackground">#0F172A</item>
    </style>

</resources>
